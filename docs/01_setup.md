# Setup

1. Create a virtual environment.
2. Install `requirements.txt`.
3. Download the Kaggle MNIST CSV files into `data/raw/`.
4. Run the notebooks in order.
5. Save the trained Keras model to `models/mnist_digit_model.keras`.
6. Start `python app.py`.
